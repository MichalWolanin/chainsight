import { sharedUtilTesting } from './shared-util-testing';

describe('sharedUtilTesting', () => {
  it('should work', () => {
    expect(sharedUtilTesting()).toEqual('shared-util-testing');
  });
});
